{
  "name": "reactive-tiles",
  "version": "0.0.1",
  "main": "js/app.js",
  "scripts": {
    "start": "mkdir dist && browserify -r react -o dist/react.js && watchify -x react -o dist/app.js -v -d ."
  },
  "browserify": {
    "transform": [
      "reactify"
    ]
  },
  "devDependencies": {
    "browserify": "^8.1.3",
    "reactify": "^1.0.0",
    "watchify": "^2.3.0"
  },
  "dependencies": {
    "react": "^0.12.2"
  }
}
